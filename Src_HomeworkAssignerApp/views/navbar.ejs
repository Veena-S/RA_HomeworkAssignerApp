<nav class="row navbar navbar-expand-sm navbar-light cust-nav-color">
  <a href="/" class="navbar-brand"><img src="/./images/homework_1.jpg" alt="HP"></a>
  <button
    class="navbar-toggler"
    data-toggle="collapse"
    data-target="#navbarMenu"
  >
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarMenu">
    <ul class="navbar-nav ml-auto ">

      <!--If roles is empty, it means no user is logged in-->
      <% if(roles.length === 0) {%> 
        <li class="nav-item">
        <a
          href="/login"
          class="nav-link cust-nav-btn"
          data-toggle="modal"
          data-target="#login-modal-form"
          >Login</a
        >
      </li>
      <%} else {%> <!--Somebody is already logged in-->
        <span class="navbar-text mr-3"> Welcome <%=userName%>!!</span>
          <li>
            <!-- <a href="/logout" class="nav-link btn-secondary btn-sm">Logout</a> -->
            <form action="/logout?_method=DELETE" method="POST">
              <button class="btn cust-nav-btn text-left">Logout</button>
            </form>
          </li>
      <%}%>
      <li class="nav-item">
        <a href="/about" class="nav-link">About</a>
      </li>
    </ul>
  </div>
</nav>
<%if(roles.length === 0) {%>
<!---->
<%-include('loginForm', { displayType: 'login', loginFormDetails : {} })-%>
<!---->
<% } else { %>
<!---->
<%-include('loginForm', { displayType: 'login-done', loginFormDetails : {} })-%>
<% }  %>
